<UserControl x:Class="TcpServerApp.NotificationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="NotificationControlRoot">
    
    <UserControl.Resources>
        <!-- Fade In Animation -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                           From="20" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- Fade Out Animation -->
        <Storyboard x:Key="FadeOutAnimation" Completed="FadeOut_Completed">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                           From="0" To="-20" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>

    <Border x:Name="NotificationBorder" CornerRadius="12" Padding="18,14" 
            Margin="0,0,0,10" Cursor="Hand" MouseLeftButtonDown="Notification_Click"
            RenderTransformOrigin="0.5,0.5" Opacity="0">
        <Border.RenderTransform>
            <TranslateTransform Y="20"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="25" ShadowDepth="5" Opacity="0.7"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Icon with Glow -->
            <Border Grid.Column="0" Width="45" Height="45" CornerRadius="10" 
                    Margin="0,0,15,0" VerticalAlignment="Center">
                <Border.Background>
                    <RadialGradientBrush>
                        <GradientStop Color="#40FFFFFF" Offset="0"/>
                        <GradientStop Color="#10FFFFFF" Offset="1"/>
                    </RadialGradientBrush>
                </Border.Background>
                <TextBlock x:Name="IconText" Text="ℹ️" FontSize="24" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText" Text="إشعار" Foreground="White" 
                           FontSize="14" FontWeight="Bold" Margin="0,0,0,4"/>
                <TextBlock x:Name="MessageText" Text="رسالة الإشعار" 
                           Foreground="#E0E0E0" FontSize="12" TextWrapping="Wrap"/>
                <TextBlock x:Name="TimeText" Text="الآن" 
                           Foreground="#999" FontSize="10" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="2" Content="✕" Click="Close_Click"
                    Width="30" Height="30" VerticalAlignment="Top"
                    Background="#20FFFFFF" Foreground="White" 
                    BorderThickness="0" FontSize="14" Cursor="Hand"
                    Margin="10,0,0,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Progress Bar -->
            <ProgressBar Grid.Column="0" Grid.ColumnSpan="3" 
                         x:Name="TimeoutProgress"
                         Height="3" VerticalAlignment="Bottom" 
                         Background="Transparent" BorderThickness="0"
                         Margin="-18,0,-18,-14" Maximum="100" Value="100"/>
        </Grid>
    </Border>
</UserControl>
